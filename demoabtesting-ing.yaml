apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demoabtesting
spec:
  rules:
  - host: "testabtesting.app.demo-orange.gocno.io"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: demoabtesting
            port:
              number: 80
